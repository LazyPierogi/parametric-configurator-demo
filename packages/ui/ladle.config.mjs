/** @type {import('@ladle/react').Config} */
const config = {
  stories: 'stories/**/*.stories.@(ts|tsx)',
  title: 'Curtain Wizard UI Kit',
  addons: {
    controls: true,
    actions: true,
  },
};

export default config;
